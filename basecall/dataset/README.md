# Dataset

Large dataset files are not stored in this GitHub repository.

Expected local files:

- Subset.pod5
- fast5_subset/Subset.fast5

Place these files in this folder before running `basecall_pipeline.pbs`.

Reason: GitHub blocks regular Git files larger than 100 MB.
